[package]
name = "multisig-authority"
version = "1.0.0"
edition = "2021"

[dependencies]
soroban-sdk = "20"
stellar-spend-shared = { path = "../shared" }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
strip = true
overflow-checks = true
