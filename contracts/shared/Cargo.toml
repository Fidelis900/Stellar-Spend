[package]
name = "stellar-spend-shared"
version = "1.0.0"
edition = "2021"

[dependencies]
soroban-sdk = "20"

[lib]
crate-type = ["rlib"]

[profile.release]
opt-level = "z"
lto = true
strip = true
overflow-checks = true
